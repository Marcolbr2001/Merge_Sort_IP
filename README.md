# Merge_Sort_IP
Merge Sort IP, it sorts 1024 elements communicating via AXI Protocol.

"Merge_SortG.vhd" is the main source code;
"Rgstr" is a register declaration as a component inside the project;
"Merge_1" is the first level of the algorithm that sorts elements 2by2;
"top_sim" is the simulation code.
